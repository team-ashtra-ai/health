
(()=>{const key='sofiati_cookie_choice';if(localStorage.getItem(key))return;const b=document.createElement('div');b.className='cookie-banner is-visible';b.innerHTML='<p>This concept stores only your cookie choice. Analytics and marketing placeholders stay inactive until consent and real IDs are added.</p><div class="section-actions"><button class="button" data-choice="reject">Reject optional</button><button class="button bronze" data-choice="accept">Accept optional</button></div>';document.addEventListener('DOMContentLoaded',()=>document.body.appendChild(b));b.addEventListener('click',e=>{const c=e.target.closest('[data-choice]');if(!c)return;localStorage.setItem(key,c.dataset.choice);b.classList.remove('is-visible')})})();
