Concept name:
30 — Method

Assigned inspiration URL:
https://www.sloaneclinic.com/

What was studied:
The concept studies the reference as a structural prompt for appointment concierge: navigation hierarchy, first-screen rhythm, service grouping, mobile menu posture, footer density and premium pacing. No protected text, brand assets, code or photography from the reference is used.

How the header differs from the other concepts:
This concept uses a concierge booking header. The header is assigned the unique runtime marker `header-30-method` and a concept-specific CSS composition.

How the hero differs from the other concepts:
The hero is shaped around appointment concierge, with concept-specific grid, image treatment, radius, motion timing and headline structure.

How the page layout differs from the other concepts:
Page sections use a unique section order marker, component grid rhythm and panel mix generated for Method. The flat pages are not routed through shared root templates.

How the mobile menu differs from the other concepts:
The mobile menu uses a booking sheet menu with local JavaScript in `js/main.js` and local markup in `partials/mobile-menu.html`.

How the footer differs from the other concepts:
The footer uses a Sloane-style footer, local contact hierarchy and concept-specific footer marker `footer-30-method`.

How the motion differs from the other concepts:
Motion is based on booking stepper. The local `main.js` sets unique menu and reveal behaviour for this concept.

How Sofiati’s brand identity was applied:
Sage green, ivory, cream, bronze/champagne accents, the Sofiati logo system, FS monogram assets, botanical imagery, clinical calm and responsible advanced aesthetic biomedicine language are applied throughout. English remains the source copy in the files, while the local concept JavaScript loads the public pages in Portuguese by default.

Why this concept is not a clone of the others:
It has its own folder, flat HTML pages, `css/style.css`, `js/main.js`, partials, copied assets, design notes, header marker, footer marker, status banner partial, mobile menu marker, hero structure, section order, language switcher and interaction mode. It does not depend on root `/css`, `/js`, `/partials` or `/assets` at runtime.
