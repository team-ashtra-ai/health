# Pre Architecture Rebuild State

Generated: 2026-06-29

## Git State

- Current branch: `sofiati-50-architecture-rebuild`
- Git status summary: clean working tree before this report was created
- Starting point: branched from `sofiati-premium-visual-dna-rescue`

## Atlas Inventory

- Concept folders under `concepts/`: 50
- Real HTML pages, excluding `partials/`: 1050
- Partial HTML files under concept `partials/`: 750
- CSS files in repo, excluding `node_modules`: 105
- JS/MJS files in repo, excluding `node_modules`: 171
- `docs/sites/[site-id]/MASTER-BRIEF.md` files: 50
- `docs/sites/[site-id]/IMPLEMENTATION-REPORT.md` files: 50
- Existing design/DNA/visual/architecture docs under `docs/script-runs/`: 14

## Current Known Visual Problems

The current branch includes the premium visual DNA rescue, so the no-crop photo baseline, sage/deep-sage/bronze rhythm, and concept-level visual documentation are present. The new architecture brief still identifies the remaining visual risk: the sites can still feel too structurally related if the same HTML anatomy, section construction, CTA model, responsive stack, and component sequence are reused beneath the visual layer.

## Current Known Architecture Problems

- The current 50-site atlas still relies on a common page-generation anatomy across all real pages.
- Section content meaning is preserved, but the presentation architecture is not yet different enough at the component/HTML/CSS/JS level.
- Existing design DNA documents are useful but too visual and not comprehensive architecture contracts.
- Partials are differentiated visually, but header/footer/mobile-menu production architecture needs to be documented and validated as a first-class system.
- Real pages need richer concept-specific architecture attributes: component type, HTML anatomy, CSS layout, responsive pattern, interaction model, CTA pattern, and image treatment.
- Architecture docs need to prove there are no duplicate component sequences, no duplicate hero/header/footer/mobile combinations, and no duplicate CTA/section rhythm contracts.
- JavaScript behaviour exists mostly through shared partial logic; concept-level interaction architecture needs to be explicit, useful, accessible, and documented without gimmicks.

This report records the pre-edit state before the architecture rebuild changes production pages, partials, scripts, shared CSS, shared JS, or generated architecture documentation.
